{{!< main}}
<div class="main_layout">
    {{> admin-sidebar }}
    <div class="content">   
        {{> admin-header }}
        <!-- other content -->
         <div class="faq_management_page common_table">
            <div class="add_user">
                <a href="/admin/gift-management/add" class="add_user_btn"><i class="fa fa-circle-plus"></i> </i>Gift Amount</a>
            </div>
            <table id="faq-management" class="display table cmn_table" style="width:100%">
                <thead>
                    <tr>
                        <th>Sr. No</th>
                        <th>Gift Amount</th>
                        <th style="width:150px;">Action</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each giftList}}
                    <tr>
                        <td>{{addOne @index}}</td>
                        <td>{{gift_amount}}</td>
                        <td> 
                            {{!-- <a href="#" class="action_btn"><i class=" fa fa-eye"></i></a> --}}
                            <a href="/admin/gift-management/edit/{{id}}" class="action_btn"><i class=" fa fa-pen"></i></a>
                            <a href="#" class="action_btn delete-gift" data-id="{{id}}"><i class="fa fa-trash-can"></i></a>
                        </td>
                    </tr>
                    {{/each}}
                
                </tbody>
            </table>        
        </div>
    </div>
</div>
{{> footer-script }}
<script>

    $(document).on('click', '.delete-gift', function(){
        var giftId = $(this).attr('data-id');
        var alertData = {
            title: 'Delete Gift Plan',
            text: 'Are you sure you want to delete this plan?', 
            btnText: 'Yes, delete',
            icon: 'warning'
        };
        var url = '/admin/gift-management/delete/'+giftId;
        alertBox(alertData, url);
    });
  
</script>