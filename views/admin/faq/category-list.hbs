{{!< main}}
<div class="main_layout">
    {{> admin-sidebar }}
    <div class="content">   
        {{> admin-header }}
        <!-- other content -->
         <div class="faq_management_page common_table">
            <div class="add_user">
                <a href="/admin/faq-category-management/add" class="add_user_btn"><i class="fa fa-circle-plus"></i> </i>FAQ</a>
            </div>
            <table id="faq-management" class="display table cmn_table" style="width:100%">
                <thead>
                    <tr>
                        <th>Sr. No</th>
                        <th>Category Name</th>
                        <th style="width:150px;">Action</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each faqCategoeyList}}
                    <tr>
                        <td>{{addOne @index}}</td>
                        <td>{{name}}</td>
                        <td> 
                            {{!-- <a href="#" class="action_btn"><i class=" fa fa-eye"></i></a> --}}
                            <a href="/admin/faq-category-management/edit/{{id}}" class="action_btn"><i class=" fa fa-pen"></i></a>
                            <a href="#" class="action_btn delete-cat" data-id="{{id}}"><i class="fa fa-trash-can"></i></a>
                        </td>
                    </tr>
                    {{/each}}
                
                </tbody>
            </table>        
        </div>
    </div>
</div>
{{> footer-script }}
<script>

    $(document).on('click', '.delete-cat', function(){
        var catId = $(this).attr('data-id');
        var alertData = {
            title: 'Delete FAQ Category',
            text: 'Are you sure you want to delete this category?', 
            btnText: 'Yes, delete',
            icon: 'warning'
        };
        var url = '/admin/faq-category-management/delete/'+catId;
        alertBox(alertData, url);
    });
  
</script>